import React from 'react'

const Owen = (setResultado) => {
    setResultado((parseFloat(peso) * 11.3) + (parseFloat(altura) * 16) / 100 + 901);
  return (
    <div>Owen</div>
  )
}

export default Owen